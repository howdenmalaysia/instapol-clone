<div class="product-item">
    <a href="{{ $url }}" class="text-decoration-none product-link">
        <img src="{{ $imagePath }}" alt="{{ $alt }}" height="{{ $height }}" class="mb-3">
        <p class="title">{{ $name }}</p>
    </a>
</div>